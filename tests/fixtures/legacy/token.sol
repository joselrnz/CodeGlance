// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

contract Token {
    function transfer(address to) public returns (bool) { return true; }
    function balance() public view returns (uint) { return 0; }
}
