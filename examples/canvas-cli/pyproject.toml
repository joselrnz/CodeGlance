[project]
name = "canvas-cli-example"
version = "0.1.0"
description = "Tiny dependency-free canvas rendering CLI used as a codeglance example."
requires-python = ">=3.10"

[project.scripts]
canvas-cli = "canvas_cli.cli:main"
