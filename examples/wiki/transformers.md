# Transformers

The transformer is a sequence model built from stacked [[Attention]] blocks and
feed-forward layers. Inputs are first turned into [[Embeddings]].

## Architecture
Encoder and decoder stacks of self-attention and MLP layers.

## Training
See [[Training]] for the optimization recipe and hyper-parameters.
