# Attention

Self-attention lets every token attend to every other token. It is the heart of the
[[Transformers]] architecture.

## Scaled dot-product
Queries, keys, and values; a softmax over scaled dot products.

## Multi-head
Several attention heads run in parallel and are concatenated.
