# Embeddings

Embeddings map discrete tokens to continuous vectors that [[Transformers]] consume.

## Token embeddings
A lookup table from vocabulary id to a dense vector.

## Positional encoding
Adds order information on top of the token [[Embeddings]].
